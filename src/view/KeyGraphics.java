package view;

import javafx.scene.Group;
import modele.Key;

public class KeyGraphics extends ItemGraphics {
    public KeyGraphics(Group parent, Key item, int scale, int x, int y) {
        super(parent, item, scale, x, y);
    }

    @Override
    public void show() {

    }
}
